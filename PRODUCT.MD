# AI Keys APP 产品说明

> 页面实现状态（2026-08-18）：**UI 已全部实现**（shadcn 风格），数据为模拟数据（`app/lib/core/providers/mock_data.dart`），真实逻辑待接入。详见下方各节标注。

# APP首页

- 顶部右上角有一个 “导入邮箱” 按钮
- 顶部有统计卡片 包含 邮箱数量 已用数量
- 卡片下方是一个分页的邮箱列表

# 导入邮箱

- 点击 “导入邮箱” 按钮，进入导入邮箱页面
- 页面有按钮可以导入CSV文件
- 页面有输入框可以输入粘贴数据
- csv文件和输入框 输入的数据格式如下：
  -- 邮箱----密码----client_id----refresh_token----创建时间
- 导入的邮箱持久化 下次进入APP展示首页卡片和列表

# 邮箱列表

- 邮箱列表展示 帐号 和 密码；帐号是邮箱号不要后缀
- 邮箱列表项可以点击，点击复制到粘贴板 格式：帐号：xxx 密码：xxx
- 邮箱列表项右边有一个“激活“按钮； 点击“激活”按钮进入激活详情页面
- 邮箱列表项左滑则是删除邮箱，弹出 二次确认 删除操作
- 已经激活的邮箱需要有一个激活状态用于区分 可用 小绿点/底色 区分

# 激活详情

- 进入激活详情页面则触发激活操作，用一个step步骤条或动画进行展示当前所在的步骤 不需要人为干预只展示 进度
- 激活操作主要分以下几步：
- 1. 发送验证码阶段
- 2. 收取验证码阶段
- 3. 脚本注册阶段
- 4. 激活认证阶段

## ！！！注意：页面风格一定要美观 可参照 shadcn，如上功能先开发页面 逻辑后续补充
