# AI Keys APP 产品说明

> 本文档对照设计稿 `light.png` / `dark.png` 编写，用于 Flutter 开发落地。
> 整体风格：简约、高级；卡片化布局、圆角、柔和阴影；交互带动效。支持浅色 / 深色 / 跟随系统。
> 全局交互：**禁用滚动回弹**（无 iOS 式 overscroll 光晕 / 拉伸）。

---

## 一、整体视觉规范（Design Tokens）

### 配色
| 用途 | 颜色 | 说明 |
| --- | --- | --- |
| 页面背景 | `#F5F6F8` | 浅灰白，略带冷调 |
| 卡片 / 表面 | `#FFFFFF` | 白色卡片 |
| 主色（Primary） | `#3B6EF5` | 主按钮、强调、激活中步骤 |
| 主色浅底 | `#EAF0FF` | 小按钮（激活/导入）背景、信息条背景 |
| 成功 / 可用 | `#22C55E` | 可用状态点、导入成功勾选 |
| 失败 / 警示 | `#EF4444` | 失败邮箱数字 |
| 中性灰（未激活） | `#9CA3AF` | 未激活状态点、次要文字 |
| 主文字 | `#1F2430` | 标题、主要内容 |
| 次要文字 | `#8A90A0` | 副标题、说明文字、占位符 |
| 分割线 / 描边 | `#EAECEF` | 卡片描边、列表分隔、虚线框 |

> 品牌色 `Primary #3B6EF5` / `成功 #22C55E` / `失败 #EF4444` 在浅色与深色下保持一致；其余语义色随主题切换。

### 深色模式配色（对照 `dark.png`）
| 用途 | 颜色 |
| --- | --- |
| 页面背景 | `#0C0E12` |
| 卡片 / 表面 | `#181B21` |
| 主色浅底 | `#1C2740` |
| 成功浅底 | `#12301E` |
| 中性灰 | `#6E7480` |
| 主文字 | `#EDEFF3` |
| 次要文字 | `#9096A2` |
| 分割线 / 描边 | `#262A31` |

### 圆角与阴影
- 卡片圆角：`16`
- 按钮圆角：`12`
- 头像：圆形
- 阴影：柔和、低透明度（如 `color: black 6%, blur: 20, offset(0,6)`）

### 字体层级
- 页面大标题（AI Keys）：约 `28` 加粗
- 卡片数字（128 / 86）：约 `26` 加粗
- 导航标题：约 `17` 中粗
- 正文：`14–15`
- 说明 / 副文字：`12–13`

### 头像规则
列表项左侧为圆形字母头像，取邮箱账号首字母（大写），底色按字母哈希在一组柔和色中循环（蓝、绿、橙、紫、青等），字母为对应深色。

---

## 二、底部导航（Bottom Navigation）

全局底部 Tab 三项：
1. **邮箱**（信封图标）— 首页 / 默认选中
2. **激活记录**（时钟图标）
3. **设置**（齿轮图标）

选中项主色高亮，未选中为中性灰。

---

## 三、首页（邮箱页）

- 顶部左侧为大标题 **AI Keys**，其下副标题「管理你的邮箱与激活状态」。
- 顶部右上角有一个 **「导入邮箱」** 按钮（主色浅底 + 主色文字 + 信封图标）。点击进入导入邮箱页面。
- 标题下方是两张并排的统计卡片：
  - **邮箱数量**：大数字（如 128），下方小字「总数量」，右侧信封图标（主色浅底圆角块）。
  - **已激活数量**：大数字（如 86），下方小字「可用数量」，右侧对勾图标（成功色浅底圆角块）。
- 统计卡片下方是 **邮箱列表** 区块：
  - 区块标题「邮箱列表」。
  - 标题下方有一个 **搜索框**，占位符「搜索邮箱或密码」，带放大镜图标；输入可实时过滤账号与密码。
  - 列表为分页展示的邮箱列表（见下节「邮箱列表」）：
    - **下拉刷新**：顶部下拉触发刷新，重新读取本地已存数据。
    - **上拉加载**：每页 12 条，滚动接近底部自动加载下一页，加载中显示「加载中...」页脚；全部载入后显示「没有更多了」。
  - 列表底部居中显示统计文案「共 N 个邮箱」。

---

## 四、邮箱列表

每个列表项（卡片行）从左到右：

1. **圆形字母头像**（账号首字母）。
2. **账号 + 状态**：
   - 上行为账号（邮箱号去掉后缀，如 `alice2026`）。
   - 下行为掩码密码（如 `********`）+ 状态点与文字：
     - **可用**：绿色小圆点 `● 可用`（已激活）。
     - **未激活**：灰色小圆点 `● 未激活`。
3. **「激活」按钮**：主色浅底 + 主色文字，点击进入激活详情页面。
4. **右箭头 `>`**：整行可点。

交互：
- **点击列表项**：复制到剪贴板，格式 `帐号：xxx 密码：xxx`，并给出复制成功提示。
- **左滑列表项**：出现删除操作，删除前 **二次确认弹窗**。
- 已激活/未激活以状态点与文字区分（绿点=可用 / 灰点=未激活）。

---

## 五、导入邮箱

顶部：返回箭头 + 居中标题「导入邮箱」。

内容分区：

1. **导入 CSV 文件**
   - 一个虚线描边的上传区域，居中上传图标 + 文案「点击或拖拽 CSV 文件到此处」，下方小字「仅支持 .csv 格式」。
   - 支持点击选择文件与拖拽导入。

2. **或 粘贴数据**
   - 多行输入框，占位提示粘贴格式：
     ```
     请粘贴数据，格式如下：
     邮箱----密码----client_id----refresh_token----创建时间

     user1@example.com----123456----xxxx----xxxx----2024-01-01 10:00:00
     user2@example.com----123456----xxxx----xxxx----2024-01-01 10:00:00
     ```
   - 右下角显示字数计数（如 `0/5000`）。

3. **格式说明**（字段对照表）
   | 字段 | 说明 |
   | --- | --- |
   | 邮箱 | 邮箱地址 |
   | 密码 | 邮箱密码 |
   | client_id | 客户端 ID |
   | refresh_token | 刷新令牌 |
   | 创建时间 | 创建时间，格式：`yyyy-MM-dd HH:mm:ss` |

   > 字段间以 `----`（四个连字符）分隔。

4. **底部**：主色大按钮 **「导入邮箱」**；按钮下方小字「导入即表示您已阅读并同意 数据隐私政策」（「数据隐私政策」为可点击链接）。

导入结果：
- CSV 与粘贴数据均按上述格式解析。
- 导入的邮箱 **持久化**，下次进入 APP 时首页统计卡片与列表展示已导入数据。
- 导入完成后进入 **导入成功页**（见第七节）。

---

## 六、激活详情

进入激活详情页面即 **自动触发激活操作**，无需人为干预，仅展示进度。

顶部：返回箭头 + 居中标题「激活详情」。

内容：

1. **邮箱信息卡片**：圆形字母头像 + 账号（`alice2026`）+ 完整邮箱（`alice2026@example.com`）。

2. **步骤时间线（Step 步骤条 / 动画）**：竖向时间线，共 4 步，每步含：图标节点、步骤名、右侧计时（进行中显示计时如 `00:08`，未开始显示 `--:--`）、状态说明文案。
   - **1. 发送验证码阶段** — 进行中（主色实心圆点 + 计时），文案「正在向邮箱发送验证码...」
   - **2. 收取验证码阶段** — 「等待接收验证码...」
   - **3. 脚本注册阶段** — 「准备执行注册脚本...」
   - **4. 激活认证阶段** — 「等待激活认证...」
   - 当前进行中的步骤高亮（主色）并显示**旋转加载环**（转圈动效）+ 中心实心点；已完成步骤显示绿色对勾完成态，未开始步骤为灰色图标。步骤切换有过渡动效。

3. **激活认证链接**（进入第 4 步「激活认证阶段」时出现）：
   - 该步骤会**获取到一个认证链接**并展示在时间线下方的卡片中（链接图标 + 「激活认证链接」标题 + 说明）。
   - 链接以主色浅底、下划线样式呈现，**可点击**；点击后进入**应用内 WebView 页面**打开该链接完成认证（顶部返回 + 标题「激活认证」+ 顶部加载进度条；加载失败可重试）。
   - 说明：认证链接最终由激活后端返回；当前为占位实现，从账号信息生成一个规范链接以打通「展示 → 点击 → WebView 打开」全流程。

4. **底部提示条**（主色浅底信息条 + 盾牌图标）：
   - 标题「激活过程中请勿退出页面」
   - 说明「请保持网络连接稳定，激活过程可能需要 1-2 分钟」

激活完成后更新该邮箱状态为「可用」，返回列表时体现绿色状态点。**第 4 步「激活认证」成功后，页面播放礼炮散花（confetti）庆祝动效。**

---

## 七、导入成功

导入邮箱完成后的结果页：

- 顶部居中 **绿色圆形对勾** 图标，带庆祝式点缀动效（散点/放射）。
- 主标题「导入成功」，副标题「共导入 N 个邮箱」。
- **结果统计卡片**（键值列表）：
  | 项目 | 值 |
  | --- | --- |
  | 新增邮箱 | 120 |
  | 更新邮箱 | 8 |
  | 失败邮箱 | 0（失败色 / 有失败时红色高亮） |
  | 可用邮箱 | 86（成功色 / 绿色） |
- 底部两个按钮：
  - 主色实心按钮 **「查看邮箱列表」**（返回首页列表）。
  - 描边/次要按钮 **「继续导入」**（返回导入页）。

---

## 八、设置页

底部导航「设置」进入。顶部为大标题「设置」，内容为分组卡片：

1. **外观**
   - 主题模式三选一：**浅色 / 深色 / 跟随系统**，选中项右侧显示对勾。
   - 深色配色对照 `dark.png`；「跟随系统」跟随设备明暗设置。
   - 选择即时生效并 **持久化**，下次启动记忆。

2. **语言**
   - 语言切换（架构已支持多语言）。当前提供 **简体中文**（选中）；English 为预留项，标注「即将支持」。
   - 选择持久化。

3. **关于**
   - 显示应用版本号（如 `v1.0.0`）。

> 说明：语言采用轻量自定义方案（词条集中管理，Provider 驱动），新增语言只需补一份词条；当前仅内置简体中文。

---

## 九、交互与动效要求

- 页面风格务必 **美观、简约、高级**。
- 关键交互带动效：
  - 页面切换转场过渡。
  - 激活详情步骤条推进动画（进行中步骤旋转加载环）。
  - 激活第 4 步认证成功的礼炮散花（confetti）动效。
  - 导入成功对勾 + 散点入场动画。
  - 列表项左滑删除、点击复制反馈（Toast / 轻提示）。
  - 按钮点击态、卡片轻微阴影/浮起。
- **滚动**：全局禁用回弹（无 overscroll 光晕 / 拉伸），滚动到边界即停。
- **深色 / 浅色**：已实现浅色 / 深色 / 跟随系统三种模式，可在设置页切换并持久化。
