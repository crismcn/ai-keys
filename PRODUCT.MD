# AI Mails APP 产品说明

> 本文档对照设计稿 `light.png` / `dark.png` 编写，用于 Flutter 开发落地。
> 整体风格：简约、高级；卡片化布局、圆角、柔和阴影；交互带动效。支持浅色 / 深色 / 跟随系统。
> 全局交互：**禁用滚动回弹**（无 iOS 式 overscroll 光晕 / 拉伸）。

---

## 一、整体视觉规范（Design Tokens）

### 配色

| 用途             | 颜色      | 说明                                |
| ---------------- | --------- | ----------------------------------- |
| 页面背景         | `#F5F6F8` | 浅灰白，略带冷调                    |
| 卡片 / 表面      | `#FFFFFF` | 白色卡片                            |
| 主色（Primary）  | `#3B6EF5` | 主按钮、强调、激活中步骤            |
| 主色浅底         | `#EAF0FF` | 小按钮（激活/导入）背景、信息条背景 |
| 成功 / 可用      | `#22C55E` | 可用状态点、导入成功勾选            |
| 已使用（橙黄）   | `#F59E0B` | 已使用状态点、右滑「设为已使用」背景 |
| 失败 / 警示      | `#EF4444` | 失败邮箱数字                        |
| 中性灰（未激活） | `#9CA3AF` | 未激活状态点、次要文字              |
| 主文字           | `#1F2430` | 标题、主要内容                      |
| 次要文字         | `#8A90A0` | 副标题、说明文字、占位符            |
| 分割线 / 描边    | `#EAECEF` | 卡片描边、列表分隔、虚线框          |

> 品牌色 `Primary #3B6EF5` / `成功 #22C55E` / `失败 #EF4444` 在浅色与深色下保持一致；其余语义色随主题切换。

### 深色模式配色（对照 `dark.png`）

| 用途          | 颜色      |
| ------------- | --------- |
| 页面背景      | `#0C0E12` |
| 卡片 / 表面   | `#181B21` |
| 主色浅底      | `#1C2740` |
| 成功浅底      | `#12301E` |
| 中性灰        | `#6E7480` |
| 主文字        | `#EDEFF3` |
| 次要文字      | `#9096A2` |
| 分割线 / 描边 | `#262A31` |

### 圆角与阴影

- 卡片圆角：`16`
- 按钮圆角：`12`
- 头像：圆形
- 阴影：柔和、低透明度（如 `color: black 6%, blur: 20, offset(0,6)`）

### 字体层级

- 页面大标题（AI Mails）：约 `28` 加粗
- 卡片数字（128 / 86）：约 `26` 加粗
- 导航标题：约 `17` 中粗
- 正文：`14–15`
- 说明 / 副文字：`12–13`

### 头像规则

列表项左侧为圆形字母头像，取邮箱账号首字母（大写），底色按字母哈希在一组柔和色中循环（蓝、绿、橙、紫、青等），字母为对应深色。

---

## 二、底部导航（Bottom Navigation）

全局底部 Tab 三项：

1. **邮箱**（信封图标）— 首页 / 默认选中
2. **激活记录**（时钟图标）
3. **设置**（齿轮图标）

选中项主色高亮，未选中为中性灰。

---

## 三、首页（邮箱页）

- 顶部左侧为大标题 **AI Mails**，其下副标题「管理你的邮箱与激活状态」。
- 顶部右上角有一个 **「导入邮箱」** 按钮（主色浅底 + 主色文字 + 信封图标）。点击进入导入邮箱页面。
- 标题下方是两张并排的统计卡片：
  - **邮箱数量**：大数字（如 128），下方小字「总数量」，右侧信封图标（主色浅底圆角块）。
  - **已激活数量**：大数字（如 86），下方小字「可用数量」，右侧对勾图标（成功色浅底圆角块）。
  - 两张卡片**可点击，与下方列表联动做状态过滤**：点「邮箱数量」显示全部邮箱；点「已激活数量」仅显示已激活邮箱，再次点击回到全部。当前生效的卡片以对应主题色描边高亮（邮箱数量=主色，已激活=成功色），默认选中「邮箱数量」。过滤与搜索框叠加生效，切换过滤时列表分页重置。
- 统计卡片下方是 **邮箱列表** 区块：
  - 区块标题「邮箱列表」。
  - 标题下方有一个 **搜索框**，占位符「搜索邮箱或密码」，带放大镜图标；输入可实时过滤账号与密码。
  - 列表为分页展示的邮箱列表（见下节「邮箱列表」）：
    - **下拉刷新**：顶部下拉触发刷新，重新读取本地已存数据。
    - **上拉加载**：每页 12 条，滚动接近底部自动加载下一页，加载中显示「加载中...」页脚；全部载入后显示「没有更多了」。
  - 列表底部居中显示统计文案「共 N 个邮箱」。

---

## 四、邮箱列表

每个列表项（卡片行）从左到右：

1. **圆形字母头像**（账号首字母）。
2. **账号 + 状态**：
   - 上行为账号（邮箱号去掉后缀，如 `alice2026`）。
   - 下行为掩码密码（如 `********`）+ 状态点与文字：
     - **可用**：绿色小圆点 `● 可用`（已激活）。
     - **已使用**：橙黄色小圆点 `● 已使用`。
     - **未激活**：灰色小圆点 `● 未激活`。
3. **「激活」按钮**：主色浅底 + 主色文字，点击进入激活详情页面。该按钮仅在邮箱**未激活**时显示；已激活邮箱不再显示。
4. **右箭头 `>`**：整行可点。

交互：

- **点击列表项**：进入该邮箱的 **邮件列表页面**（查看收取到的邮件，见第四节之一）。
- **左滑列表项**：出现删除操作（红色底 + 垃圾桶图标），删除前 **二次确认弹窗**。
- **右滑列表项**：按状态推进一步（绿色/橙黄色底 + 对勾图标），操作前均有 **二次确认弹窗**，确认后状态点变色并轻提示：
  - **未激活 → 已激活（可用）**：绿色，提示「已设为激活状态」。
  - **已激活（可用）→ 已使用**：橙黄色，提示「已设为已使用状态」。
  - **已使用**：为终态，不再支持右滑，仅保留左滑删除。
- 三种状态以状态点与文字区分：**灰点=未激活**、**绿点=可用（已激活）**、**橙黄点=已使用**。

---

## 四之一、邮件列表

从「邮箱列表」点击某一邮箱项进入，展示该邮箱 **收取到的邮件**。

顶部：返回箭头 + 居中标题「邮件列表」。

内容：

- 顶部 **邮箱信息卡片**：圆形字母头像 + 账号 + 完整邮箱。
- **邮件列表卡片**：数据来自邮件后端 `POST /api/imap/fetch_emails`（分页），每封邮件一行，从左到右为发件方字母头像、发件方名称（未读加粗）、右侧时间与 **未读小圆点**（主色）；下方为主题（两行）与发件邮箱地址（次要文字）。
  - **点击邮件**：弹出 **邮件详情抽屉弹窗**（底部上滑面板），调用 `POST /api/imap/fetch_email_detail`（#3）获取正文，并将该邮件 **标记为已读**（未读圆点消失、名称恢复常规字重）。
  - **下拉刷新**：重新从第 1 页收取邮件，加载期间显示「正在收取邮件...」。
  - **上拉加载**：每页 10 条，接口返回 `has_more` 为真时滚动接近底部自动加载下一页，加载中显示「加载中...」页脚；全部载入后显示「没有更多了」。
  - 列表底部居中显示「共 N 封邮件」（N 取接口 `total`）；无邮件时居中显示空状态「暂无邮件」；网络失败显示错误态与「重试」。

**邮件详情抽屉**：底部圆角面板（约 88% 高度，顶部拖动条 + 标题 + 关闭按钮），内容为发件方头像/名称/时间、收件人、发件人、分割线，其下为**正文渲染区**；加载中/失败分别显示加载态与「重试」。详情打开成功即触发已读标记。

- **正文渲染**：接口返回的邮件多为 **HTML 正文**，详情抽屉在内嵌 **WebView** 中 **渲染 HTML**（响应式适配面板宽度，图片按宽自适应缩放，链接为主色，背景/文字色随浅色 / 深色主题切换）。出于安全考虑，**禁用 JavaScript**（邮件 HTML 为不可信内容）。
- **无 HTML 时回退**：若该邮件仅有纯文本正文（无 `html`），则展示 **可选择的纯文本正文**；两者皆无时显示「暂无正文」。

> 说明：邮件数据由 `fetch_emails`（列表，分页）与 `fetch_email_detail`（正文）接口返回；列表接口不含正文，行内副文本展示发件邮箱地址。**由于列表无稳定 ID，已读标记以「邮箱地址 + 邮件时间」哈希为键（`id = md5(邮箱 + date)`）**，持久化于本地；该键的两个字段均来自列表接口，故每次加载列表都能与本地已读记录比对回填（正文哈希做不到，因列表不含正文）。

---

## 五、导入邮箱

顶部：返回箭头 + 居中标题「导入邮箱」。

内容分区：

1. **导入 CSV 文件**
   - 一个虚线描边的上传区域，居中上传图标 + 文案「点击或拖拽 CSV 文件到此处」，下方小字「仅支持 .csv 格式」。
   - 支持点击选择文件与拖拽导入。

2. **或 粘贴数据**
   - 多行输入框，占位提示粘贴格式：

     ```
     请粘贴数据，格式如下：
     邮箱----密码----client_id----refresh_token----创建时间

     user1@example.com----123456----xxxx----xxxx----2024-01-01 10:00:00
     user2@example.com----123456----xxxx----xxxx----2024-01-01 10:00:00
     ```

   - 右下角显示字数计数（如 `0/5000`）。

3. **格式说明**（字段对照表）
   | 字段 | 说明 |
   | --- | --- |
   | 邮箱 | 邮箱地址 |
   | 密码 | 邮箱密码 |
   | client_id | 客户端 ID |
   | refresh_token | 刷新令牌 |
   | 创建时间 | 创建时间，格式：`yyyy-MM-dd HH:mm:ss` |

   > 字段间以 `----`（四个连字符）分隔。

4. **底部**：主色大按钮 **「导入邮箱」**；按钮下方小字「导入即表示您已阅读并同意 数据隐私政策」（「数据隐私政策」为可点击链接）。

导入结果：

- CSV 与粘贴数据均按上述格式解析。
- 导入的邮箱 **持久化**，下次进入 APP 时首页统计卡片与列表展示已导入数据。
- 导入完成后进入 **导入成功页**（见第七节）。

---

## 六、激活详情

进入激活详情页面即 **自动触发激活操作**，无需人为干预，仅展示进度。四个步骤由真实后端接口串行驱动（见 `API.MD`），非模拟计时：

- **1. 发送验证码阶段** → `GET /api/verification`（cun.ai），请求向邮箱下发验证码。
- **2. 收取验证码阶段** → 每 5 秒轮询一次 `POST /api/imap/fetch_emails`（持续约 5 分钟），找到主题为「CUN.AI邮箱验证邮件」**且邮件时间晚于进入激活页时刻**的邮件（避免误取上一次的旧验证码），读取详情并提取验证码。
- **3. 脚本注册阶段** → `POST /api/user/register`（cun.ai），提交邮箱 / 用户名（邮箱前缀）/ 密码 / 验证码等完成注册。
- **4. 激活认证阶段** → 每 5 秒轮询一次邮件（持续约 5 分钟），找到主题以「Verify your email to claim」开头**且时间晚于进入激活页时刻**的邮件，从其详情中提取认证链接。

> 说明：`turnstile`（人机验证）当前传空字符串；某步失败（网络 / 超时 / 后端校验不通过）时，时间线该步进入**失败态**（失败色叉号节点 + 错误文案 + 「重试」按钮），点击「重试」从该步继续。

顶部：返回箭头 + 居中标题「激活详情」。

内容：

1. **邮箱信息卡片**：圆形字母头像 + 账号（`alice2026`）+ 完整邮箱（`alice2026@example.com`）+ 右侧箭头 `>`；**整卡可点击，跳转到该邮箱的邮件列表页面**。
   - **左滑**：将该邮箱标记为**激活状态（可用）**（成功色底 + 对勾图标），操作前**二次确认弹窗**，确认后轻提示「已设为激活状态」。
   - **右滑**：将该邮箱标记为**已使用**（橙黄色底 + 图标），操作前**二次确认弹窗**，确认后轻提示「已设为已使用状态」。
   - 两种滑动均为**状态推进操作，不会移除卡片**（确认后卡片回弹）。

2. **步骤时间线（Step 步骤条 / 动画）**：竖向时间线，共 4 步，每步含：图标节点、步骤名、右侧计时（进行中显示计时如 `00:08`，未开始显示 `--:--`）、状态说明文案。
   - **1. 发送验证码阶段** — 进行中（主色实心圆点 + 计时），文案「正在向邮箱发送验证码...」
   - **2. 收取验证码阶段** — 「等待接收验证码...」
   - **3. 脚本注册阶段** — 「准备执行注册脚本...」
   - **4. 激活认证阶段** — 「等待激活认证...」
   - 当前进行中的步骤高亮（主色）并显示**旋转加载环**（转圈动效）+ 中心实心点；已完成步骤显示**淡蓝色**对勾完成态（与主色呼应，非绿色），节点间连线为**淡蓝色虚线**；未开始步骤为灰色图标、连线为灰色虚线；**失败步骤为失败色（红）叉号节点 + 红色错误文案 + 「重试」按钮**。步骤切换有过渡动效。

3. **激活认证链接**（进入第 4 步「激活认证阶段」并成功提取后出现）：
   - 该步骤会从认证邮件中**提取到认证链接**并展示在时间线下方的卡片中（链接图标 + 「激活认证链接」标题 + 说明）。
   - 链接以主色浅底、下划线样式呈现，**可点击**；点击后进入**应用内 WebView 页面**打开该链接完成认证（顶部返回 + 标题「激活认证」+ 顶部加载进度条；加载失败可重试）。链接右侧有**复制按钮**，点击将认证链接复制到剪贴板并轻提示。
   - 说明：认证链接由激活后端通过邮件下发，App 轮询收取邮件并从正文中提取该链接。

4. **底部提示条**（主色浅底信息条 + 盾牌图标）：
   - 标题「激活过程中请勿退出页面」
   - 说明「请保持网络连接稳定，激活过程可能需要 1-2 分钟」

激活完成后更新该邮箱状态为「可用」，返回列表时体现绿色状态点。**第 4 步「激活认证」成功后，页面播放庆祝动效：彩色纸屑从两侧中下（约 2/5 高度）位置持续向中上喷射并回落（confetti），配色采用与主题呼应的蓝色系为主的柔和色。**

---

## 七、导入成功

导入邮箱完成后的结果页：

- 顶部居中 **绿色圆形对勾** 图标，带庆祝式点缀动效（散点/放射）；进入页面同时播放与激活成功一致的彩色纸屑（confetti，从两侧向中上喷射并回落）。
- 主标题「导入成功」，副标题「共导入 N 个邮箱」。
- **结果统计卡片**（键值列表）：
  | 项目 | 值 |
  | --- | --- |
  | 新增邮箱 | 120 |
  | 更新邮箱 | 8 |
  | 失败邮箱 | 0（失败色 / 有失败时红色高亮） |
  | 可用邮箱 | 86（成功色 / 绿色） |
- 底部两个按钮：
  - 主色实心按钮 **「查看邮箱列表」**（返回首页列表）。
  - 描边/次要按钮 **「继续导入」**（返回导入页）。

---

## 八、设置页

底部导航「设置」进入。顶部为大标题「设置」，内容为分组卡片：

1. **外观**
   - 主题模式三选一：**浅色 / 深色 / 跟随系统**，选中项右侧显示对勾。
   - 深色配色对照 `dark.png`；「跟随系统」跟随设备明暗设置。
   - 选择即时生效并 **持久化**，下次启动记忆。

2. **语言**
   - 语言切换（架构已支持多语言）。提供 **简体中文** 与 **English** 两种语言，点击即时切换整个应用文案，当前语言右侧显示对勾。
   - 选择持久化。

3. **关于**
   - 显示应用版本号（如 `v1.0.0`）。

> 说明：语言采用轻量自定义方案（词条集中管理，Provider 驱动），新增语言只需补一份词条；当前内置简体中文与英文（`AppStrings` / `AppStringsEn`）。

---

## 九、交互与动效要求

- 页面风格务必 **美观、简约、高级**。
- 关键交互带动效：
  - 页面切换转场过渡。
  - 激活详情步骤条推进动画（进行中步骤旋转加载环，完成态淡蓝色对勾、节点间淡蓝色虚线）。
  - 激活第 4 步认证成功的庆祝动效：主题蓝色系彩色纸屑从两侧中下（约 2/5 高度）位置持续向中上喷射并回落（confetti）；导入成功页复用同一效果。
  - 导入成功对勾 + 散点入场动画。
  - 列表项左滑删除、点击复制反馈（Toast / 轻提示）。
  - 按钮点击态、卡片轻微阴影/浮起。
- **滚动**：全局禁用回弹（无 overscroll 光晕 / 拉伸），滚动到边界即停。
- **深色 / 浅色**：已实现浅色 / 深色 / 跟随系统三种模式，可在设置页切换并持久化。
