# DESIGN.md — AI Keys 设计契约

> 本文件是长期设计契约。每次 UI 会话第一条：读本文件并遵守；新工作在此之上扩展，
> 不得静默违背。变更设计决策时，在同一提交内更新本文件。

## 设计读取（Design Read）

**Reading this as:** 一个邮箱账号注册机的移动端管理界面，面向单一用户（自用），Android，
**Glass & Futuristic（霓虹玻璃）+ 扁平化** / **Smooth & Premium** 动效，
**Utility** 型（工具，设计服务于任务），dials **V4 / M4 / D5**。

- **Register**：Utility。设计服务于「更快地导入、激活、管理账号」。玻璃质感用于品牌记忆，
  不是炫技；动效克制（productive 优先）。
- **App kind**：Productivity / tool → 四叶草（VARIANCE 4 / MOTION 4 / DENSITY 5）。

## 视觉方向

- **风格**：霓虹玻璃（Neon Glass）+ 扁平化。深空/亮白基底上漂浮靛蓝·紫·青光斑；
  卡片半透明纯色底、**无边框线**（区分靠透明度与距离，不靠描边）、大圆角；
  渐变 + 辉光只用于品牌元素（按钮/头像/统计卡/步骤高亮），业务内容保持纯色克制。
- **主主题**：**双主题同等**（跟随系统 + 手动手势切换，偏好持久化）。先调浅色再推深色，
  两套都实机核对，不许「只编译不核对」。
- **品牌色**：靛蓝 `#6366F1`（主）→ 紫 `#8B5CF6`（渐变）；成功 **翠绿 `#10B981`**，
  危险 **红 `#EF4444`**。一个重点色体系锁定全 App；辅色仅作语义（成功/危险/警告）。
- **动效个性**：**Smooth & Premium**。整页转场 300–400ms 强调曲线；就地变化 ≤300ms；
  入口编排只做首页一处；退出比进入快；`MediaQuery.disableAnimationsOf` 必被尊重。
- **文案语气**：**Minimal 简洁克制**（Apple / Notion 式）。按钮 1–3 词动词开头，
  不加戏、不卖萌、错误无感叹号、生成文案内禁用破折号（导入格式分隔符 `----` 除外）。

## 三个拨盘

| 拨盘 | 值 | 理由 |
|---|---|---|
| DESIGN_VARIANCE | 4 | Utility 工具：一致性优先，玻璃风格本身已构成差异，不做逐屏艺术导演 |
| MOTION_INTENSITY | 4 | Smooth & Premium：首页一侧入口编排 + 全 App 秒级转场/微交互；不喧兵夺主 |
| VISUAL_DENSITY | 5 | 列表工具中等密度；数据区用留白分组而非卡片套卡片（禁止嵌套卡片） |

## 一号动作（Q5）

**激活邮箱账号。** 唯一的实色渐变高反差操作族 = 「激活」；其余（导入 / 清除搜索 / 返回）
降级到 outline / tonal / text。首页把「待激活」数量做成可见状态（表示 Zeigarnik 动机），
激活进入即开始、步骤条显示进度、成功有 ≤1 次的庆祝时刻（≤300ms 完成微确认）。

## 决策日志

- 2026-08-19：按 beautify-flutter skill `craft` 确立本契约。从「shadcn 风格」迁移为
  「霓虹玻璃 + 扁平化」；增加主题三选切换器并持久化；确立 Smooth & Premium 动效基线。